



# override url for tomcat since glassfish lives at 8080
squid:

    DEV:

       url: http://localhost:9090


# override only one parameter, we should get the non-local settings for the others
jira:

    TEST:

      port: 8888



# qa and dev BSPs not working for me right now and I'm only doing light read-only stuff so just point to PROD
mercury:

    TEST:

      bsp: PROD
